  <%= image_tag(@photo.picture_url, width: 900) if @photo.picture.present? %>

<p>
  <strong>Name:</strong>
  <%= @photo.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @photo.description %>
</p>
by: <%= @photo.user.email %>
<p>

  <br>
  <hr>
  <h3>Comments</h3>
<% @comments.each do |comment| %>
  <div>
    <strong><%= comment.user_name %></strong>
    <br>
    <p><%= comment.body %></p>
    <p><%= link_to 'Delete', comment_path(comment), method: :delete, data: { confirm: 'Are you sure?' } %></p>
  </div>
<% end %>
<%= render partial: 'comments/form', locals: { comment: @comment } %>
</p>
<% if user_can_edit_b? %>
<%= link_to 'Edit', edit_photo_path(@photo) %> |
<%end%>
<% if user_can_destroy_b? %>
<%= link_to 'Delete', @photo, data: { confirm: 'Are you sure?' }, method: :delete %> |
<%end%>
<%= link_to 'Back', photos_path %>
